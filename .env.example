# Travel API credentials — keep these server-side
TRAVELPAYOUTS_API_TOKEN=
TRAVELPAYOUTS_MARKER=

# Optional: URL for your chosen flight-search endpoint.
# Keep this in the server/API layer, not public JavaScript.
TRAVELPAYOUTS_FLIGHTS_URL=
